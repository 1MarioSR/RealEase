    @model PaymentsViewModel

@{
    ViewData["Title"] = "Payment List";


}



<h2 class="text-primary">All Payments</h2>
<a class="btn btn-primary" id="addPaymentButton"> Add </a>



<div class="card shadow-sm">
    <div class="card-body">
        <h4 class="text-secondary">All Payments</h4>
        <table class="table table-striped table-hover text-center">
            <thead class="table-dark">
                <tr>
                    <th>Id</th>
                    <th>ContractId </th>
                    <th>TenantId</th>
                    <th>PaymentDate</th>
                    <th>Amount</th>
                    <th>PaymentMethod</th>
                    <th>Status</th>
                    <th>Actions</th>
                </tr>
            <tbody id="PaymentsTableBody">
                <tr>
                    <td colspan="8">No Records found</td>
                </tr>
            </tbody>
                @* @if (Model.AllPayments != null && Model.AllPayments.Any())
                {
                    @foreach (var Payment in Model.AllPayments)
                    {
                        <tr>
                            <td>@Payment.Id</td>
                            <td>@Payment.Name</td>
                            <td>@Payment.Lastname</td>
                            <td>@Payment.Address</td>
                            <td>
                                @if (Payment.IsActive)
                                {
                                    <span class="badge bg-success">Active</span>
                                }
                                else
                                {
                                    <span class="badge bg-secondary">Inactive</span>
                                }
                            </td>
                            <td>
                                <a class="btn btn-warning btn-sm" asp-action="Edit" asp-route-id="@Payment.Id">
                                    <i class="bi bi-pencil-square"></i> Edit
                                </a>
                                <a class="btn btn-info btn-sm" asp-action="Details" asp-route-id="@Payment.Id">
                                    <i class="bi bi-info-circle"></i> Details
                                </a>
                                <a class="btn btn-danger btn-sm" asp-action="Delete" asp-route-id="@Payment.Id">
                                    <i class="bi bi-trash"></i> Delete
                                </a>
                            </td>
                        </tr>
                    }
                }
                else
                {
                    <tr>
                        <td colspan="7" class="text-muted">No Payments found.</td>
                    </tr>
                }
            </tbody>*@      
            </table>
    </div>
</div>


@* <div class="card shadow-sm mt-5">
    <div class="card-body">
        <h4 class="text-secondary">Active Payments</h4>
        <table class="table table-striped table-hover text-center">
            <thead class="table-dark">
                <tr>
                    <th>Id</th>
                    <th>Name</th>
                    <th>Lastname</th>
                    <th>Address</th>
                    <th>Is Active</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @if (Model.ActivePayments != null && Model.ActivePayments.Any())
                {
                    @foreach (var Payment in Model.ActivePayments)
                    {
                        <tr>
                            <td>@Payment.Id</td>
                            <td>@Payment.Name</td>
                            <td>@Payment.Lastname</td>
                            <td>@Payment.Address</td>
                            <td>
                                <span class="badge bg-success">Active</span>
                            </td>
                            <td>
                                <a class="btn btn-warning btn-sm" asp-action="Edit" asp-route-id="@Payment.Id">
                                    <i class="bi bi-pencil-square"></i> Edit
                                </a>
                                <a class="btn btn-info btn-sm" asp-action="Details" asp-route-id="@Payment.Id">
                                    <i class="bi bi-info-circle"></i> Details
                                </a>
                                <a class="btn btn-danger btn-sm" asp-action="Delete" asp-route-id="@Payment.Id">
                                    <i class="bi bi-trash"></i> Delete
                                </a>
                            </td>
                        </tr>
                    }
                }
                else
                {
                    <tr>
                        <td colspan="7" class="text-muted">No active Payments found.</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div> 
 </table> *@

@section Scripts
{
    <script>

        $(document).ready(function () {
            loadPayments();

            $('#addPaymentButton').click(function () {
                window.location.href = 'https://localhost:7146/Payments/Create';
            });

            $(document).on('click', '.delete-Payment', function () {
                var PaymentId = $(this).data('id');
                if (confirm('Are you sure you want to delete this Payment?')) {
                    $.ajax({
                        url: 'https://localhost:7012/Payment/DeletePayment/' + PaymentId,
                        type: 'DELETE',
                        success: function () {
                            alert('Payment deleted successfully');
                            loadPayments();
                        },
                        error: function (error) {
                            console.log('Error:', error);
                        }
                    });
                }
            });

            $(document).on('click', '.edit-Payment', function () {
                var PaymentId = $(this).data('id');
                window.location.href = 'https://localhost:7146/Payments/Edit/' + PaymentId;
            });

        });

        function loadPayments() {
            $.ajax({
                url: 'https://localhost:7012/Payment/GetPayments',
                type: 'GET',
                success: function (data) {
                    var tableBody = '';
                    if (data.length === 0) {
                        tableBody = '<tr><td colspan="8">No Records found</td></tr>';
                    } else {
                        $.each(data, function (index, Payment) {
                            tableBody += '<tr>' +
                                '<td>' + Payment.id + '</td>' +
                                '<td>' + Payment.contractId + '</td>' +
                                '<td>' + Payment.tenantId + '</td>' +
                                '<td>' + Payment.paymentDate + '</td>' +
                                '<td>' + Payment.amount + '</td>' +
                                '<td>' + Payment.paymentMethod + '</td>' +
                                '<td>' + Payment.status +
                                '<td>' +
                                '<a class="btn btn-warning edit-Payment" data-id="' + Payment.id + '">Edit</a> ' +
                                '<button class="btn btn-danger delete-Payment" data-id="' + Payment.id + '">Delete</button>' +
                                '</td>' +
                                '</tr>';
                        });
                    }
                    $('#PaymentsTableBody').html(tableBody);


                },
                error: function (error) {
                    console.log('Error:', error);
                }
            });
        }

    </script>

}